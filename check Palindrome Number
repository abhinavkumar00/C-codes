#include<stdio.h>
int main()
{
	int n,a,r=0,p;
	printf("Enter any Number: ");
	scanf("%d",&n);
	p=n;
	while(n!=0)
	{
		a=n%10;
		n=n/10;
		r=r*10+a;
	}
	if(p==r)
		printf("It is a Palindrome Number");
	else
		printf("It is not a Palindrome Number");
	return 0;
}
